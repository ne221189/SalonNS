<%= image_tag "logo.PNG", size: "180x48", alt: "Salon NS" %>
<% if current_owner %>
  <%= current_owner.salon ? current_owner.salon.name : "無所属" %>
  <ul class="account-menu">
    <%= current_owner.name + "さん" %>
    <%= menu_link_to "ログアウト", :owner_session,
                     method: :delete, data: { confirm: "ログアウトしますか?" } %>
  </ul>
<% end %>

<%= render "shared/owner_menubar" %>