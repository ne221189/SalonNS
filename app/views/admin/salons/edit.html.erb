<% @page_title = "美容院情報の編集" %>

<h1><%= @page_title %></h1>

<div class="toolbar"><%= link_to "一覧に戻る", :admin_salons %></div>

<%= form_for [:admin, @salon] do |form| %>
  <%= render "admin/shared/admin_salon_form", form: form %>
  <div><%= form.submit "登録" %></div>
<% end %>